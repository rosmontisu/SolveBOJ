#include <iostream>
#include <vector>
using namespace std;

vector<int> adj[40000];
int indegree[40000];



int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n, m; // 문제의 수, 정보의 수
    for (int i = 0; i < m; i++)
    {
        int a, b;
        cin >> a >> b;
        adj[a]
    }
}