#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    int n; cin >> n;
    vector<int> v(n * 2 + 1, -1);
    vector<bool> vis(n * 2 + 1, false);

    for (int i = 1; i <= n; i++)
    {
        
    }

}