#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int dp[2002][2002]; // pair형태로 s,e 에 따른 답을 저장하면 될듯

int main()
{
    cin.tie(0);
    ios::sync_with_stdio(0);

    int n; 
    cin >> n;
    for (int i = )
    
}