#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    ios::sync_with_stdio(false); 
    cin.tie(NULL);
    
    int g; 
    cin >> g;
    /*
    g = now^2 - before^2
    before^2 = now^2 - g 

    g = (now+before)(now-before)
    */

    return 0;
}
