#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    ios::sync_with_stdio(false); 
    cin.tie(NULL);
    
    int g; 
    cin >> g;

    /*
    g = now^2 - before^2

    // g는 주어지고, now를 찾아야하므로

    before^2 = now^2 - g 
    (단, now >= before)
    (before은 자연수여야 함)
    */

   for (int i = )


    
    return 0;
}
